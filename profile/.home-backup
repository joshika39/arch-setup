#!/bin/bash

MAIN_DIR=/home/$USER/
subdir=home/

GIT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )/$subdir" 
homesub=
backups=( 
	".fehbg"
	".xinitrc"
	".bashrc"
	".Xresources"
	".colors")
